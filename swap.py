from collections import Counter

def swap(input_file):
  try:
    
    # run until quit

    # read and display input_file content

    # prompt user to swap a character

    # when given a swap command, iterate over file content and swap each instance

    # redisplay the message and repeat

  except FileNotFoundError:
    print("EXCEPTION: File not found")


# Run from terminal (eg. python frequency.py)
if __name__ == '__main__':
  swap('test.text')